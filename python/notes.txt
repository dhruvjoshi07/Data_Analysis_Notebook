`											EASY
-multi line comments are not done in python but we can use by docs string
*3rules we cant use as a variable
	=1avx // starting no numbers
	=a b  //we cant use spaces
	=special char in var(@,$,#)	//not used 
*Naming Conventions
	=camel case	//not space but capitalize first alphaword of second,ex.AbcDef
	=pascal case	//abcDec	,a is small 
	=snake case	//ab_cd		,'_' use this
*complex data type 'v=34j'	//only j 
*boolean,b= true
*to find unique code,
	a="A"
	print(ord(a))  and use 'chr' by replacing ord we get unique code 
*String Indexing
	=positive indexing=0 1 2 3 4
	=negative indexing=-4 -3 -2 -1 0
	print(a[1])
*String Slicing[start:stop:steps]  //if no value then it will go default to end
*type conversion
	a=str(12)
	print(a)
*bool
a=12
print(bool(a))
#7 values that gives false are -false,0,0.0,"",[],(),{}
*formatting string
	print(f"abc{a}and bcd{b}")
*input function
	input("abc")	//this is default string
	for int, int(input(""))
#for square, print(5**2)
*conditional statement
	if a<b:
		print()
	elif a==b:
		print()
	else:
		print()
#for loop works when we know the limit and while loop until it done
*range function
	range(start,stop,steps)
*for loop
	a=range(1,20,1)	//for reverse step-1
	for i in a:
#que:print table of 5,			//for i in range(n,n*10+1,n):
	for i in range(5,51,5):
		print(i)
		print(i)
*loops for strings
	a.using index values	
		a="abc"
		for i in range(len(a)):
			print(i)
	b.iterating directly over the string
		a="abc"
		for i in a:
			print(i)
*while loop
a=1
while a<=30(this is condition):
	print(a)
	a=a+1
#seaparate each digit of a number and print it on the new line.
#accept a number and print it reverse
#accept a number and check if it is a palindrome number
#create a random number guessing game with python

*FUNCTIONS
Parameters=the thing u accept
Arguments=the thing u provide to parameters
-types=	def hello(name,age):
		print(f"your name is {name} and your age is {age}")
	hello(age=22,name='abc')	//key argument

	def hello(name,age=17):
		print(f"your name is {name} and your age is {age}")
	hello('abc')			//default argument and neche val dalne se update

	def hello(name,age):
		print(f"your name is {name} and your age is {age}")
	hello('abc',18)			//positional argument

														MEDIUM
*DataStructure
-List
	syntax: a=[1,2,3...]
	-mutable,duplicates,ordered,heterogenous(multiple datatypes) 
	-indexing and slicing
Traversing list
-for i in range(len(a)):
	print(a[i])
//shortcut to access elements without indexing
for i in a:
	print(i)
+=METHODS ,print(dir(list)) //ismei sare list k matods dikh jayenge
+=help(list)	//yea btayega methods mei kya use hota
='append' use case ,last mei add krne k liye,	l=[1,2,3,4]
						l.append(6)
						print(l)
=l.insert(1,2)		//1 is index and 2 is value
=extend=multiple elements in list
=l.remove(2)		//first occuraance delete 2 ki

-Tuple
	syntax:	a={1,2,3}
	-immutable,duplicates,ordered,heterogenous
+=Methods
first	a={1,2,3,4,5,5,5,5}
	-index=a.index(5)
	print(index)		//value kis index pr hai
second
	count=a.count(5)
	print(count)

Tuple Unpacking
	a={1,2,3,4}
	then we give var to each val like
	a,b,c,d={1,2,3,4}
	print(a)		output=1

=que? 	a={1}
		this is integer like a=1 its not tuple so that to ake it tuple
		a={1,}		//it makes tuple

-Set
	syntax: s={}	//this is dictionary not set
			s={1,2,3,4}
	-mutable,no duplicates,unordered(no indexes access val),semi-heterogenous(string,number,tuple but not everything)
+=Note: Hash function?
		value kahi bhi add ho skti set mei
+=Methods:	//it imp cuz it provide mutable 
	s=(1,2,3,4)
	s.add(4)		//adds an element
	s.remove(2)		//remove 2 but raise error
	s.discard(5)	//remove without error
	popped_element=s.pop{}		//remove random element
	s.clear{}		//remove all elements

	a={1,2,3}
	b={3,4,5}
	union_set=a.union(b)	//{1,2,3,4,5}
	intersection_set=a.intersection(b)	//{3}
	difference_Set=a.difference(b)	//{1,2}
	symmetric_diff=a.symmetric_difference(b)	//{1,2,4,5}

-Dictionary /hash map(in other lang)
	-d={//key and values ka pair }
	d={
		1:"hello",		//1 is key,"hello" is pair
		2:56
		}
	print(d[1])		//it acts like index in list
	d[1] = 1000 	//updation in values 
	d[3]=10		`	//creation khud python samjh jyega 3 number ki key hai ni
	del d[3]		//deletion
	print(d)
	-mutable,duplicates,order,heterogenous
	-key not change and it ust unique
	-it follows insertion order // key k upar val based hoti
+=Traversing
	d={1:"hello",2,:40,3:"a"}
	for i in d:		//it do iteration in keys
		print(d[i])	//without d gives key and with d gives value
or
	for in d.values():		without writing values it default key
		print(I)
+=Methods
	help(dict)
que?Deep copy
	a=[1,2,3,4,5]
	b=a.copy		//swallow copy
	b=a
	b[0]=100
	print(a)		//it gives [100,2,3,4,5] cuz b mei changes a mei ho gya

#write a py script to merege 2 py dictionaries
#write a py program to sum all the values in a dictionary
#count the frequency of each elements
#write a python program to combine 2 dictionary by adding values for common keys


														HARD
*EXCEPTTION HANDLING
-errors
	2 types-syntax error,indentation errpr
	due to mistakes in the code that prevent it from running.
Exceptions keyword:
	-try,except,else,finally,raise	//try alwys run except
	example,	try:
					print(10/a)
				except Exception as err:
					print(f"Sorry u cant divide by 0 it gives {err}")
				else:
					print("This done as a replacemnt of except")
				print("ok i have the division")

	-finally:
		print("Ok it will run no matter what")

	-raise example	//manually throw exception
try:
	if age<10 or age >18:
		raise ValueError("error")		//it will stop excution then we use try
	else:
		print("abccc")
except Execption as err:
	print({err})

print("abc")		//ab yea line print hogi

*FILE HANDLING
-Creating,Reading,updating,deleting
-open function=open()	//give path
	p=open(r'main.py')
	print(p.read())

-multiple modes to open the file
	'r'-read but file must exist
	'w'-write it create file or overwrites
	'a'-append it adds to end of file
	'x'-create a new file,fails if it exist
example,r=open("superman.txt",'w')
		r.write("abc")
		r.close()

<<<<<<< HEAD

															INTERESTING
+=OOPS(Object oriented program)
*imperative approach (ex,a=1 b=2 print(a+b))
*functional approach (ex, def add(a,b) print(a+b))
*oops approach

-CLASSES(blueprint or templete for objects )
	Syntax: class abc:
				//attributes and methods 
				a=12		//attriibute
				
				def hello():		//methods
					print("Abx")
				print("cde")
			print(abc().a)		//a ko call krna
			abc().hello()		//fx calling

-OBJECTS
		Syntax: class abc:
				//attributes and methods 
				a=12		//attriibute
				
				def hello(self):		//methods
					print("Abx")
				print("cde")
			obj=abc()
			print(obj.a)	or	obj.hello()

-CONSTRUCTOR
		//that runs automatically when we calls class and target the object location
		class factory:
			def_init_(self,material,zips,pockets):	//dender method
			// self location farget target 	
				self.material=material
				self.zips=zips
				self.pockets=pockets

			def show(self):
				print(f"your objects details are{self.material},{self.zips},{self.pockets})

		rebok=factory("leather",3,2)	//rebok is object of company of leather
		campus=factory("nylon",3,3)		//second object

		rebok.show()

-TYPES OF ATTRIBUTE
.Class Attribute-A normal var created inside a class is a class attribute

.Instance Attribute-Self.name,self.age,etc

-TYPES OF METHODS
.Instance Method,access and modify 
	Jab aapko kisi specific student ka data (name, marks) handle karna ho.
.Class Method,use decorater(@)
	@classmethod
	def hello(cls):		//cls target class location
		print("abc")

		note:Jab aapko poori class ki property (college name) modify karni ho.
.Static Mthod,no class na class,simple regular fx 
	@staticmethod
	def static():
		print("How are u")

	obj=Animal(12)
	obj.show() or obj.hello

	note:Jab aapko ek aisa helper function chahiye jo class ke andar toh hai, par usse
	 data se koi lena dena nahi (like simple math).



								
							4-Pillars
-Inheritance
Syntax:
	class factory:				#parent class
		a="attribute"
		def hello(self):
			print("method")

	class factorypune(factory)	#child class
		pass

	obj=factory()
	obj2=factorypune(obj2.hello)
	
	print(obj.hello)
=Constructor 
	class Animal
		def _init_(self,name):
			self.name=name
		def show(self):
			print(f"hello your name is {self.name}")
	
	class Human(Animal):
		pass

	person1=Human("dhruv")
	person2=Anmal("Muskan")

	person1.show()
*super fx()=it target parent class 
	class Animal:
		def _init_(self,name):
			self.name=name
		def show(self):
			print(f"hello your name is {self.name}")
	
	class human(animal):
		def _init_(self,name,age):
			super()._init_(name)
			self.age=age
		def show(self):
			print(f"hello your name is {self.name}",{self.age})

	animal1=Animaal("lion")
	person=Human("abc",23)

=Types of Inheritance
-Single inheritance
-multiple inheritance
	//there will be 2 parent class and one child and child have both access of parent class
class Animal:
	name1="lion"
class Human:
	name2="harsh"
class Robots(Animal,Human):
	name3="charlie123
obj=Robots()
print(obj.name1)

or

class Animal:
	def __init__(self,name)
	pass
class Human:
	def __init__(self,name,age)
	pass
class Robots(Animal,Human):	//animal aage rakhne se bs name dikhega print mei 
							//but Human pehle rakhne se name,age dono puchega
	name3="charlie123
obj=Robots(	)

-Multilevel Inheritance
	//grandparents class then parent class then child class
class factory:
	def __init__(self,material,zips):
		self.material=material
		self.zips=zips
class factorybhopal(factory):
	def __init__(self,material,zips,color):
		super().__init__(material,zips)
		self.color=color
class factorypune(factorybhopal):
	def __init__(self,material,zips,color,pockets):
		super.__init__(material,zips,color)
		self.pockets=pockets

obj=factorypune()

-Hierachial inheritance
	//ek class 2 child



-POLYMORPHISM
	//a same have diferent forms
IMPORTANT NOTE:
	THERE ARE TWO TYPES OF POLYMORPHISM BUT COMPILE TIME LANGUAGE(like c++,java) THERE ARE 3 WAYS 
	BECAUSE PYTHON DOESNOT SUPPORT METHOD OVERLOADING
+=METHOD OVERRIDING
class Animal:
	def show(Self):
		print("abc")

//method overriding
class Human(Animal):
	def show(self)
		print("cde")
 
obj=Human()
obj.show()

+=DUCK TYPING
class Animal:
	def show(Self):
		print("abc")

class Human:
	def show(self)
		print("cde")

obj=Animal()
obj2=Human()

obj.show()
obj2.show()




-ENCAPSULATION
	//it means putting variables and fx together in one piece
why?	//it keeps data safe from being changed mistakely
		//it gives ctrl over what others can access or change
class factory:
	a="pune"

	def show(self):
		print("abc)
=+Access modifiers
	means how we give access of our attribute and methods to object or inherited class
	-types:
		-public attribute
		-protected attribute method = _a ,'_'lga do 
			//baki lang mei access ni kr payenge but py mei naming convention ki tarh use
		-private attribute method = __a,'__' yea access ni r payenge
		example,	class factory:
					__a="pune"

					def __show(self):
						print("abc")
					obj=factory()
					obj.__show()



-ABSTRACTION
	//it does not in python but we can use by library
	from abc import ABC, abstractmethod
	class abstract(ABC):	//ABC is a helper 
		@abstractmethod		//decorater
		def perimeter(self):
			pass
		@abstractmethod
		def area(self):
			pass
	class square:
		def __init__(self,side):
			self.side=side
	class circle(abstract):
		def __init__(self,radius):
			self.radius=radius
		def perimeter(self):
			print("abc")
		def area(self):
			print("abc")
	obj=circle(7)
		
													DUNDER METHODS
-that start and end with double underscores,like__init__,__str__,__add__,etc.
-they help u:
	customize behaviour of class
	make class objects behave like built-in data types(like list,str)
=+METHOD 1:	__STR__
class Animal:
	def __init__(self,name):
		self.name=name
	def__str__(self):
	return f"abc{self.name}"
obj=Animal("lion")
print(obj)
=yea method seedha object print kr deta

=+METHOD2:	__ADD__	#want to add 2 integers of 2 objects
class Animal:
	def __init__(self,name,age):
		self.name=name
		self.age=age
	def__str__(self):
		return f"abc{self.name}"
	def __add__(self,other):
		return f"sum of ages are {self.age+other.age}"
obj=Animal("lion",22)
obj2=Animal("dolphin",23)
print(obj + obj2)

											ADVANCED
-DECORATOR								
A fx that modifies another fx without changing changing its actual code
def decorate(func):
	def wrapper(*args):			//*args so bar bar change na krne pade
		print("i will print befor hello fx")
		func()
		print("i will print after hello fx")
	return wrapper
@decorate
def hello():
	print("hello this is dhruv")
hello()


-Args(*)
	def addition(a,b,c,d)
		print(a+b)
	addition(12,12,23,56)
	+=Ab user kitne bhi value mang skta bar bar use kr ni skte change in fx
	thats why ha use krte '*args' ki le lo jitni value leni user ko add k liye
	#ek tuple form k andar ho jayega
	def addition(*args):
		sum=0
		for i in args:
			sum=sum+i
		print(sum)

	addition(12,12,23,45,23.........)

-Kwargs(keyword args)(**)
def information(**kwargs):
	for i in kwargs:
		print(f"{i}:{kwargs[i]}")
information(name="dhruv",age=23,desigination="ai/ml")
//yea hammei dicitionary form mei dega

-List,Dictionary and set comphrehension
//we dont have to write multiple line of code for loops and conditions
#MAKE LIST FROM 1 TO 20
l=[i for i in range(1,21) if i%2==0]
print((l))
#DICTIONARY from 1 to 10 and sqaure them as values of keys
L={i:i**2 for i in range{1,10}}
print(l)
#set
l={i*i for i in range(10)}
print(l)

-LAMBDA FUNCTIONS
lambda keyword
	syntax:	add=lambda a,b : a+b
	//we can use if else and ternary 
			print(add(2,3))


//this is not useful from here
Dhruv joshi
muskan bhardwaj dixit
to here
>>>>>>> a6b5a1ba04e92fe8773235405a0ad281f2483db1
