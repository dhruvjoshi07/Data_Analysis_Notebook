*Notebooks
A notebook is a special file(usually .ipynb) that lets:
    write code
    run code
    see output
    add notes,equations,images and charts-all ine one place

*jupter kholo terminal mei
    -cd "pth of folder"
    -type = jupyter notebook
    -new pr jakr python

or folder pr click krke terinal mei bs jupyter notebook likh do

+=Jupyter 
-shift+enter        //generate output

-Basic array creation
    import numpy as np

-Basic Array creation
    //it create using list
    arr=np.array([1,2,3,4])
    print(arr)

in list,
    a=[2,3,4,"a]
in np.array
    error

*vectors and metrices
vectors-1-d , [3,2,4,5]
metrics-multiple list , [1,2,3,4],[4,5,6],[5,6,1,2]
but list doesnt support multiple list
np.arrar(list)
//2d mei tenors hote hai

*for range 
    np.arrage(1,11)

-np.arange(start, stop, step): Yahan aap khud batate ho ki gap kitna chahiye
 (e.g., "har 2 number baad point lagao").

-np.linspace(start, stop, num): Yahan aap batate ho ki total kitne points chahiye,
aur NumPy gap (step) apne aap calculate kar leta hai.

*linespace
    yea hamesha deafult value 50 leta agar kuch na daale toh and it takes float val
    